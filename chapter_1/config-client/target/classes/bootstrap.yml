#spring:
#  application:
#    name: config-client
#  cloud: #不注册到eureka
#    config:
#      uri: http://localhost:9655
#      fail-fast: true
#  profiles:
#    active: dev


#############注册到eureka
spring:
  application:
    name: config-client
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: config-server
  profiles:
    active: dev
  rabbitmq:
    host: 123.207.37.241
    port: 5672
    username: guest
    password: guest
#配置中心mq http://localhost:9656/bus/refresh 刷新配置

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9647/eureka/
management:
  security:
    enabled: false
